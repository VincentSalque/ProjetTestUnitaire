using Hardware;
using MachineACafé.Test.Utilities;
/*
namespace MachineACafé.Test;

public class CoinCodeTest
{
    [Fact]
    public void CoinCode_ShouldHaveExpectedValues()
    {
        // Arrange & Act
        var coinCodes = Enum.GetValues(typeof(CoinCode)).Cast<CoinCode>().ToList();

        // Assert
        Assert.Contains(CoinCode.Cent1, coinCodes);
        Assert.Contains(CoinCode.Cent2, coinCodes);
        Assert.Contains(CoinCode.Cent5, coinCodes);
        Assert.Contains(CoinCode.Cent10, coinCodes);
        Assert.Contains(CoinCode.Cent20, coinCodes);
        Assert.Contains(CoinCode.Cent50, coinCodes);
        Assert.Contains(CoinCode.Euro1, coinCodes);
        Assert.Contains(CoinCode.Euro2, coinCodes);
    }
}
*/